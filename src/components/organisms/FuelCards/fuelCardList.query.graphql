query GetFuelCards($data: FuelCardInputFilter!) {
  fuelCards(data: $data) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      cursor
      node {
        ...FuelCardItemFields
      }
    }
  }
}
