query GetUpcomingTachoCalibration(
  $data: UpcomingTachoCalibrationInput!
  $first: Int
  $last: Int
) {
  upcomingTachoCalibration(data: $data, first: $first, last: $last) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      cursor
      node {
        id
        registration
        tachoCalibration
        type
      }
    }
  }
}
