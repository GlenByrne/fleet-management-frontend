query GetUsersOrganisations($first: Int, $after: String) {
  usersOrganisations(first: $first, after: $after) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      cursor
      node {
        role
        organisation {
          id
          name
        }
      }
    }
  }
}
